def texto( frase = "Estou com muito sono"):
    return frase
print(texto())

''' A função chamada texto que recebe um argumento opcional chamado frase, com um valor padrão "Estou com muito sono". Essa função simplesmente retorna a frase que for passada para ela como argumento ou, caso nenhum argumento seja passado, retorna a frase padrão.

Quando a função texto() é chamada sem nenhum argumento, ela utiliza o valor padrão "Estou com muito sono" e retorna essa frase. Posteriormente, o print(texto()) exibe essa frase que foi retornada pela função.

Então, no final, o código imprime a frase "Estou com muito sono".'''